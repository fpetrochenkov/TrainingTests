<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>

	<properties resource="_database.properties">
		<property name="username" value="username" />
		<property name="password" value="password" />
		<property name="url" value="url" />
		<property name="driver" value="driver" />
	</properties>

	<typeAliases>
		<typeAlias alias="CarTire" type="com.qaprosoft.models.CarTire" />
		<typeAlias alias="TyreTire" type="com.qaprosoft.models.TyreTire" />
		<typeAlias alias="OffRoadTire" type="com.qaprosoft.models.OffRoadTire" />
		<typeAlias alias="FreightTire" type="com.qaprosoft.models.FreightTire" />
	</typeAliases>

	<environments default="development">
		<environment id="development">
			<transactionManager type="JDBC" />
			<dataSource type="POOLED">
				<property name="driver" value="${driver}" />
				<property name="url" value="${url}" />
				<property name="username" value="${username}" />
				<property name="password" value="${password}" />
			</dataSource>
		</environment>
	</environments>

	<mappers>
		<mapper resource="mybatis/mappers/CarTire_Mapper.xml" />
	
	</mappers>

</configuration>